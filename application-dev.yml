config:
 info: this is master branch , application-dev.yml ,version = 1

web:
 site: http://www.zuowenjun.cn,http://zuowj.cnblogs.com--20190227
 skills: .net,java,html,js,css,sql,python,vb--20190227
 motto: Learning is endless; Opportunity is for the prepared mind;--20190227

server:
  port: 80001

logging:
  level:
    com:
      example:
        ms:
          dao: debug
mybatis:
  configuration:
    default-fetch-size: 100
    default-statement-timeout: 3000
    map-underscore-to-camel-case: true
  mapperLocations: classpath:mappers/*.xml
  type-aliases-package: com.d.ms.shopping.domain
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/miaosha?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false
    username: mldn
    password: mldn
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    initialSize: 1
    maxActive: 2
    maxOpenPreparedStatements: 20
    maxWait: 60000
    minEvictableIdleTimeMillis: 300000
    minIdle: 1
    poolPreparedStatements: true
    testOnBorrow: false
    testOnReturn: false
    testWhileIdle: true
    timeBetweenEvictionRunsMillis: 60000
    type: com.alibaba.druid.pool.DruidDataSource
    validationQuery: select 'x'
